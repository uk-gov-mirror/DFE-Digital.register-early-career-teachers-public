<% page_data(title: "Authorise #{@client.name} to access your ECTs") %>

<p class="govuk-body">
  <%= @client.name %> has asked for access to early career teacher (ECT) data for <%= @appropriate_body.name %>.
</p>

<p class="govuk-body">
  If you authorise this, <%= @client.name %> will be able to view and record induction data for your ECTs.
</p>

<div class="govuk-button-group">
  <%= button_to "Authorise", oauth_authorization_path, class: "govuk-button", data: { module: "govuk-button" } %>
  <%= button_to "Cancel", oauth_authorization_path, method: :delete, class: "govuk-button govuk-button--secondary", data: { module: "govuk-button" } %>
</div>
